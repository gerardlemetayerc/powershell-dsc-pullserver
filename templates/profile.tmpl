{{define "content"}}
<section class="content">
    <div class="container-fluid">
        <h2>User profile</h2>
        <div class="card mb-4">
            <div class="card-body">
                <p><b>Mail :</b> <span id="profileEmail"></span></p>
                <p><b>Name :</b> <span id="profileName"></span></p>
            </div>
        </div>
        <div class="card">
            <div class="card-header">
                <b>Tokens API</b>
                <button class="btn btn-sm btn-success float-right" id="addTokenBtn">Add token</button>
            </div>
            <div class="card-body">
                <table class="table table-bordered" id="apiTokensTable">
                    <thead>
                        <tr><th>Label</th><th>Active</th><th>Creation date</th><th>Revocation date</th><th>Actions</th></tr>
                    </thead>
                    <tbody></tbody>
                </table>
                <div id="tokenPlainDiv" style="display:none;" class="mt-3">
                  <label for="tokenPlainCode" class="mb-2 d-block"><b>New token</b></label>
                  <div class="border rounded bg-light p-2 d-flex align-items-start justify-content-between" style="gap:12px;">
                    <pre class="mb-0 flex-grow-1" style="white-space:pre-wrap;word-break:break-all;"><code id="tokenPlainCode"></code></pre>
                    <button type="button" class="btn btn-outline-secondary btn-sm" id="copyTokenBtn">Copy</button>
                  </div>
                  <small id="copyTokenFeedback" class="form-text text-muted mt-2"></small>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Modal création token -->
<div class="modal fade" id="createTokenModal" tabindex="-1" role="dialog" aria-labelledby="createTokenModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="createTokenModalLabel">Create new API token</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form id="createTokenForm">
          <div class="form-group">
            <label for="tokenLabel">Label</label>
            <input type="text" class="form-control" id="tokenLabel" name="label" required />
          </div>
          <button type="submit" class="btn btn-primary">Create token</button>
        </form>
      </div>
    </div>
  </div>
</div>
<script src="/web/profile.js"></script>
{{end}}
