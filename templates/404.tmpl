{{ define "content" }}
<div class="container">
  <div class="card mt-5 shadow-sm">
    <div class="card-body text-center">
      <h1 class="display-4 text-warning">404 - Page non trouvée</h1>
      <p class="lead">La page demandée n'existe pas ou a été déplacée.</p>
      <a href="/web" class="btn btn-primary mt-3">Retour à l'accueil</a>
    </div>
  </div>
</div>
{{ end }}
